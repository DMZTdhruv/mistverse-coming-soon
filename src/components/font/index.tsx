import localFont from "next/font/local";

export const clgkFont = localFont({
  src: "./clash-grotesk.ttf",
  display: "swap",
  variable: "--font-clgk",
});

export const cldsFont = localFont({
  src: "./clash-display.ttf",
  display: "swap",
  variable: "--font-clds",
});
